// enums.ts

// Enum for wallet types
export enum WalletType {
    WEB3 = 'WEB3', // Standard Web3 wallet like MetaMask
    MINIMAL = 'MINIMAL', // Minimal account abstraction wallet
  }
  
  // Enum for transaction statuses
  export enum TransactionStatus {
    PENDING = 'PENDING',
    COMPLETED = 'COMPLETED',
    FAILED = 'FAILED',
  }
  
  // Enum for supported blockchain networks (example)
  export enum SupportedNetwork {
    ETHEREUM = 'ETHEREUM',
    BASE = 'BASE',
    GOERLI = 'GOERLI', // Binance Smart Chain
  }
  

  